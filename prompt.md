You are a senior full-stack engineer. I have an existing Next.js (App Router) + TypeScript + Supabase + AssemblyAI project(this codebase) using the **pre-recorded (serverless) transcription flow**.

I want to **add a toggleable streaming transcription mode** (experimental) that:
1. Uses **AssemblyAI Realtime Streaming API** via WebSocket (direct from browser)
2. **Saves every transcript (partial + final)** to a Supabase `transcription` table 
3. **Displays live text** using **Supabase Realtime (`postgres_changes`, just like the currrent pre-recorded version)** — NOT Broadcast
4. **Keeps full compatibility** with the current pre-recorded flow
5. Is **controlled by a single toggle** (`NEXT_PUBLIC_USE_STREAMING=true/false`)
6. **Shares components** (`MicButton`, `TranscriptDisplay`) between both modes
7. **Persists transcription history** across refresh/tab close
8. **Uses a `session_id` to group transcripts per recording session** just like the current pre-recorded version.

---

### REFERENCE DOCUMENT ATTACHED:(Unify and use already existing components where possible, else create new files at appropriate folders)
-Assemblyai-streaming.md
→ Contains:
- All code files (lib/, app/api/, hooks/, components/)
- Supabase table schema(adjust for compartibility with current database schema, add rows if necessary)
- Toggle logic
- Webhook → Supabase insert()
- Realtime subscription with `postgres_changes`

---

### TASK: ONE-SHOT CODEBASE UPDATE

Using the **exact code and structure** from the attached document, **update my current codebase** as follows:

#### 1. **Add new files or unify with existing files where necessary** (create if missing):

lib/config.ts
lib/supabase.ts
lib/assembly.ts
app/api/token/route.ts
app/api/ingest/route.ts
hooks/useTranscription.ts
components/DebugToggle.tsx


#### 2. **Existing pre-recorded trascription files and folders (Unify and use already existing components where possible, else create new files at appropriate folders) **:
- app\session (client mic)
- app\api\transcription\transcribe
- services\transcription
- app\api\transcription\callback
- hooks\use-realtime-transcriptions.ts
- components\realtime-transcription-display.tsx
- Database_Schema
- Pre-recorded-transcription-flow.md (just for reference )



//Ensure Supabase table exists (run SQL if needed):
create table transcripts (
  id bigint generated by default as identity primary key,
  session_id text,
  text text,
  is_final boolean,
  words jsonb,
  created_at timestamptz default now()
);
grant insert on transcripts to anon;
alter publication supabase_realtime add table transcripts;

//Environment Variables (add to .env.local and Vercel):
NEXT_PUBLIC_USE_STREAMING=false   # toggle here

Keep pre-recorded flow 100% intact when USE_STREAMING=false

Note: Use proper commenting to distinguish between the two systems where posible.
- Refer to this document as often as needed.