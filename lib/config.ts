// lib/config.ts
// Toggle: true = streaming, false = prerecorded
// Controls which transcription mode is used throughout the application

export const USE_STREAMING = process.env.NEXT_PUBLIC_USE_STREAMING === 'true';

